# Vinayaka Football

    npm install
    npm run dev

Open the printed local URL. All audio is synthesized in code (src/main.js, AUDIO section), so no audio files are needed. Character art is drawn in code (function dh); to use image sprites, put them in a `public/` folder and draw them in place of dh().
